import { motion } from "framer-motion";
import styled from "styled-components";

export const CalenderContainer = styled(motion.div)`
  background: #fafafa;
  border-radius: 4px;
  padding: 50px 24px;
`;

export const CalenderCardTop = styled.div`
  margin-bottom: 20px;
`;

export const CalenderTitle = styled.div`
  font-weight: 600;
  font-size: 23px;
  color: #171b03;
`;

export const CalenderParagraph = styled.div`
  font-weight: 400;
  font-size: 16px;
  color: #747668;
`;
